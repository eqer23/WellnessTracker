package edu.iu.wellnessTracker;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WellnessTrackerApplication {

	/*
	TODO
	Implement the actual webpage into the code. Currently I have it implemented in a basic way using the templates and the MvcConfig.
	Not sure how it works with React.
	 */

	public static void main(String[] args) {
		SpringApplication.run(WellnessTrackerApplication.class, args);
	}

}
